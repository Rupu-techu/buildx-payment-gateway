import Checkout from "../pages/Checkout.jsx";

function App() {
  // App stays intentionally small so future routing can be added
  // without changing the payment page component structure.
  return <Checkout />;
}

export default App;
